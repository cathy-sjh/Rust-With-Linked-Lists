[package]
name = "an_ok_binary_tree"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
an_ok_stack = {path = "../an_ok_stack"}
an_unsafe_queue = {path = "../an_unsafe_queue"}
